services:
  medicy_backend:
    build:
      context: C:/Users/ASUS/Desktop/Medicy/backend
      dockerfile: C:/Users/ASUS/Desktop/Medicy/backend/Dockerfile
    container_name: medicy_backend
    ports:
      - "8000:8000"
    
    # 🔽🔽🔽 [จุดแก้ไขที่ 1] 🔽🔽🔽
    # เปลี่ยน 'depends_on' ธรรมดา ให้เป็นแบบที่รอ "healthcheck"
    depends_on:
      medicy_db:
        condition: service_healthy # (รอจนกว่า medicy_db จะ healthy)
        
    environment:
      - DB_HOST=medicy_db
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=root
      - DB_NAME=medicy

  medicy_db:
    image: mysql:8.0
    container_name: medicy_db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: medicy
    ports:
      - "3307:3306"
      
    # 🔽🔽🔽 [จุดแก้ไขที่ 2] 🔽🔽🔽
    # เพิ่ม "healthcheck" เพื่อให้ Docker รู้ว่า
    # MySQL พร้อมใช้งานจริงๆ หรือยัง (โดยการ ping)
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5